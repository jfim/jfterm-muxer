// TODO(Task 5): real impl — replace this stub with the full StatusSnapshot
#[allow(dead_code)]
#[derive(Clone, Copy)]
pub struct StatusSnapshot {
    pub running: bool,
    pub progress: Option<u8>,
}
